import { ScreenPlaceholder } from "@/components/scaffold/ScreenPlaceholder";

export default function Page() {
  return (
    <ScreenPlaceholder
      screenId="SCREEN_47"
      title="Cadastro do lojista"
      description="Início da criação do restaurante no MenuNext: dados do responsável e credenciais de acesso."
      backHref="/"
      backLabel="Voltar para a página inicial"
    />
  );
}
